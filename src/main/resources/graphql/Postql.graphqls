type Post{
    id: ID !,
    title: String !,
    category: String,
    authorId: String
}

type Query {
    posts(count: Int):[Post]
    post(id: ID):Post
}

type Mutation {
    createVehicle( title: String!, category: String, authorId: String):Post
}