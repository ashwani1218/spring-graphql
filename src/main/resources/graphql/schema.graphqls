type Query {
    posts : [Post]
    post(id: ID) : Post
}
type Post{
    id: ID !,
    title: String !,
    category: String,
    authorId: String
}

type Mutation {
    createVehicle( title: String!, category: String, authorId: String) : Post
}